# Project instructions

See `INSTRUCTIONS.md` for the full weekly lab-meeting-report workflow (folder
structure, template, style rules, page-budget rules, pandoc conversion).

## Git commits and pull requests

- Never add a `Co-Authored-By: Claude` trailer to commit messages, and never
  add a "Generated with Claude Code" line (or similar) to commit messages or
  pull request descriptions. The user has explicitly and repeatedly asked for
  this across multiple sessions, commits in this repo should read as their
  own work only.
- Commit author/committer identity for this repo is `molion097
  <molionsurya55@gmail.com>` (already set via local `git config user.name` /
  `user.email` in this repo, not global). If a fresh clone doesn't have it
  set, set it locally before committing, don't rely on machine-default
  identity.
